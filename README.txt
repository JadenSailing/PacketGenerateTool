目录说明：
\packet 消息的定义
\output 输出lua文件
\src 工具源码
\python38 python3库

典型格式 参见"TestPacket.txt"
每个配置文件可以根据模块需求配置多条消息
但是由于项目设定，每条消息会单独生成一个lua文件
生成的路径可以自定义

相对宽松的格式要求：
{}用于消息或结构设置
""用于设置字符串默认值
//用于注释
除此之外不要使用其它符号，空格任意

支持特性如下
	*支持全数据类型及对应数组
	*支持自定义结构体及对应数组
	*支持结构体嵌套
	*支持消息头注释和属性的行尾注释 "//"
